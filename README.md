# Built-in to Xteink

1. **Download** the `index.html`.
2. **Toggle** SD card option.
3. **Upload** the `index.html` file into root directory and save as `xtfe.html`.
4. **Access** from `http://192.168.3.3/xtfe.html`
